/* Test struct declaration and dot operator assignment and access */

struct Book {
	int Id;
	string Title;
};

struct Doc {
	string File;
	int Word_count;
};

int main()
{
  struct Book b1;
  struct Doc doc;
  b1.Id = 2;
  b1.Title = "hello world";
  doc.File = "this is an essay";
  doc.Word_count = 20;
  print(b1.Id);
  printstr(b1.Title);
  printstr(doc.File);
  print(doc.Word_count);
  return 0;
}
